function(x, units, data=NULL) { if (!is.numeric(x) || length(x)==0L || !is.character(units) || length(units)==0L) stop('invalid grid unit'); structure(list(value=as.numeric(x), units=rep_len(units,length(x)), data=data), class='unit') }
