[package]
name = "r-graphics-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "R graphics engine - device driver interface"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
