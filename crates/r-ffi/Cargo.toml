[package]
name = "r-ffi"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-or-later"

[dependencies]
libc = { workspace = true }

[features]
default = []

[lib]
crate-type = ["rlib", "staticlib"]
