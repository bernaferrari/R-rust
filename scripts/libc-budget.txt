# libc budgets for scripts/libc_ratchet.sh — counts only go DOWN.
# Zeroed rows are hard requirements (de-libc stages A-D + the string-mem
# and FILE* waves); the two nonzero rows are the single justified
# libcurl FFI cluster (CURLOPT_WRITEDATA takes a real libc FILE*).
# Comment-only lines are excluded by the gate.
#
# category<TAB>budget
variadic_printf	0
heap	0
env	0
time_fns	0
c_type_aliases	0
string_mem	1
stdio_FILE	4
