cat(paste(sort(c("banana", "apple", "cherry")), collapse = "|"), "\n")
cat(paste(sort(c("banana", "apple", "cherry"), decreasing = TRUE), collapse = "|"), "\n")
cat(paste(sort(c("delta", "alpha", "beta")), collapse = "|"), "\n")
