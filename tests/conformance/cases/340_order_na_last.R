show <- function(x) cat(paste(x, collapse = "|"), "\n", sep = "")
show(order(c(3, NA, 1)))
show(order(c(3, NA, 1), na.last = FALSE))
show(order(c(3, NA, 1), na.last = NA))
show(order(c(3, NA, 1), decreasing = TRUE))
show(order(c("b", NA, "a")))
show(order(c("b", NA, "a"), na.last = FALSE))
show(order(c("b", NA, "a"), na.last = NA))
show(order(c(TRUE, NA, FALSE), na.last = TRUE))
